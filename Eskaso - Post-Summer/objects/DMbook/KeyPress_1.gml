if !keyboard_check_pressed(vk_escape) and !keyboard_check_pressed(ord("N")){
if visible==false visible=true; 
else 
{
if textcount >= string_length(text[i]) and hit==false
{if i<38{
	if i==27 and text[27]=="It will make you suffer, all the while whispering sweet promises of happiness..."
	{textcount=0; text[27]="...while feeding you just enough energy to make one more step towards the oblivion.";}
	else
    i+=1;
	
textcount=0;}
else room_goto_next();}
else textcount=string_length(text[i]);
}}