if k.hit==true image_speed=0 else image_speed=spd;
if k.FLY==false prev=k.cur;

if FLY==true
{
y = (cur.y+to.y)/2 + counter*counter*counter;
counter += ((abs(way)/way)*2*power(abs(way),1/3))/hopspd; //abs(x)/x = mirror
countercounter+=1;

if countercounter>hopspd+1
{FLY=false;
switch(room){
	case StagnantThoughtsYearnForCatharsis: sprite_index=s_urumagh;
	}

cur=instance_nearest(x,y,DD);
//lower=cur.lower;
//upper=cur.upper;
to=noone;}
}