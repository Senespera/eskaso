if prev.upper != noone and FLY==false and k.hit==false and k.stpd==false 
if stpd==false {//same for w
	to=k.cur;
	if room==StagnantThoughtsYearnForCatharsis
	 if to.y > cur.y sprite_index=s_urumaghdown else sprite_index=s_urumaghup;
	way=(to.y-cur.y)/2;
	counter=(abs(way)/way*-1)*power(abs(way),1/3);
	countercounter=1;
	FLY=true;
}
else cur=prev;